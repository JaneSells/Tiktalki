{
  "name": "tunetube-converter",
  "version": "1.0.0",
  "description": "A YouTube to MP3 converter application",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",           // Starts the application
    "dev": "nodemon app.js",           // Development script with nodemon
    "build": "npm install",            // Ensures dependencies are installed before deployment
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["youtube", "mp3", "converter", "express"],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.4.1",
    "ejs": "^3.1.9",
    "express": "^4.18.2",
    "node-fetch": "^2.7.0"
  }
}
